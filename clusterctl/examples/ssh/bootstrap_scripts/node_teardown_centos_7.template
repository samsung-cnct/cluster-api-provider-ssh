
          set -e
          set -x
          (
          ${FUNCTIONS}

          if ! teardown; then
            echo >&2 "Teardown unsuccessful."
            exit 68
          fi
          ) 2>&1 | tee -a /var/log/teardown.log
