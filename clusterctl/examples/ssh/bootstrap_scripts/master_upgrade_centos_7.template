
          set -e
          set -x
          (
          ${FUNCTIONS}

          if ! yum_upgrade_master; then
            echo >&2 "FATAL: upgrade failed. Quitting."
            exit 117
          fi
          ) 2>&1 | tee -a /var/log/upgrade.log
