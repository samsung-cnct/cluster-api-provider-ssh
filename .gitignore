# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Vendor directory
vendor
# binary directory
bin

# lock file for dep file
Gopkg.lock

# pivoted kubeconfig
kubeconfig
minikube.kubeconfig

